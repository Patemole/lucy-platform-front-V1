import React from 'react';
import { Box } from '@mui/material';

const HousingTinder: React.FC = () => {
  return (
    <Box sx={{ flexGrow: 1, padding: 2, backgroundColor: 'lightblue' }}> {/* Added some basic styling */}
      Component 1 (Tinder-like)
    </Box>
  );
};

export default HousingTinder; 